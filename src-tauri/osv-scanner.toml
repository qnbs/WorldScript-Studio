# osv-scanner ignore list for src-tauri/Cargo.lock + pnpm-lock.yaml
# All entries have been evaluated and accepted by the maintainer.
# Reviewed: 2026-05-17 (Rust) / 2026-09-08 (npm)

# ─── GTK3 Rust bindings (Linux WebKit stack) ────────────────────────────────
# webkit2gtk 2.0.2 (required by wry 0.55.x / Tauri 2.x for Linux) depends on
# the 0.18.x series of GTK3 Rust bindings. These crates are marked "unmaintained"
# in the advisory DB because they were superseded by the gtk4 crate series.
# No patched 0.18.x version exists and upgrading to 0.19+ would require a major
# breaking change in Tauri's Linux WebView stack (webkit2gtk → webkit2gtk-4.1).
# These crates only affect Linux desktop builds; the PWA is unaffected.

[[IgnoredVulns]]
id = "RUSTSEC-2024-0411"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "gdkwayland-sys 0.18.x: unmaintained GTK3 Wayland sys binding; no fix available without upgrading WebKit stack on Linux"

[[IgnoredVulns]]
id = "RUSTSEC-2024-0412"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "gdk 0.18.x: unmaintained GTK3 Rust binding; no fix available without upgrading WebKit stack on Linux"

[[IgnoredVulns]]
id = "RUSTSEC-2024-0413"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "atk 0.18.x: unmaintained GTK3 accessibility binding; no fix available without upgrading WebKit stack on Linux"

[[IgnoredVulns]]
id = "RUSTSEC-2024-0414"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "gdkx11-sys 0.18.x: unmaintained GTK3 X11 sys binding; no fix available without upgrading WebKit stack on Linux"

[[IgnoredVulns]]
id = "RUSTSEC-2024-0415"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "gtk 0.18.x: unmaintained GTK3 Rust binding; no fix available without upgrading WebKit stack on Linux"

[[IgnoredVulns]]
id = "RUSTSEC-2024-0416"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "atk-sys 0.18.x: unmaintained GTK3 accessibility sys binding; no fix available without upgrading WebKit stack on Linux"

[[IgnoredVulns]]
id = "RUSTSEC-2024-0417"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "gdkx11 0.18.x: unmaintained GTK3 X11 Rust binding; no fix available without upgrading WebKit stack on Linux"

[[IgnoredVulns]]
id = "RUSTSEC-2024-0418"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "gdk-sys 0.18.x: unmaintained GTK3 sys binding; no fix available without upgrading WebKit stack on Linux"

[[IgnoredVulns]]
id = "RUSTSEC-2024-0419"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "gtk3-macros 0.18.x: unmaintained GTK3 macro crate; no fix available without upgrading WebKit stack on Linux"

[[IgnoredVulns]]
id = "RUSTSEC-2024-0420"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "gtk-sys 0.18.x: unmaintained GTK3 sys binding; no fix available without upgrading WebKit stack on Linux"

[[IgnoredVulns]]
id = "RUSTSEC-2024-0429"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "glib 0.18.5 (CVSS 6.9, GHSA-wrw7-89jp-8q8g): memory safety issue in GTK3 glib Rust binding; only exploitable on Linux desktop via WebKit process; glib 0.22.x fixes it but requires webkit2gtk 4.1+ which Tauri 2.x does not yet support on this platform. Track upstream: https://github.com/tauri-apps/wry/issues"

# ─── proc-macro-error ────────────────────────────────────────────────────────
# Build-time only procedural macro helper; no runtime footprint.
# The 1.0.4 release is the final version; the crate is archived on crates.io.
# No CVE score — advisory classification is "unmaintained".

[[IgnoredVulns]]
id = "RUSTSEC-2024-0370"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "proc-macro-error 1.0.4: unmaintained build-time proc-macro helper; no runtime exposure; no newer version exists"

# ─── paste ───────────────────────────────────────────────────────────────────
# Build-time declarative-macro helper (token pasting) pulled in transitively by
# the Tauri/Rust macro stack. Advisory classification is "unmaintained" — no CVE,
# no CVSS score, no fixed version (1.0.15 is the final release; crate is archived).
# Compile-time only; there is no runtime footprint or exploitable attack surface.

[[IgnoredVulns]]
id = "RUSTSEC-2024-0436"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "paste 1.0.15: unmaintained build-time proc-macro helper; no runtime exposure; final version, no fix available"

# ─── unic-* Unicode data crates ──────────────────────────────────────────────
# unic-* 0.9.0 is the final version of this crate series (archived).
# Advisory classification is "unmaintained" — no CVE, no CVSS score.
# These provide static Unicode data tables; there is no exploitable attack surface.

[[IgnoredVulns]]
id = "RUSTSEC-2025-0075"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "unic-char-range 0.9.0: unmaintained Unicode data crate; final version; no exploitable attack surface"

[[IgnoredVulns]]
id = "RUSTSEC-2025-0080"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "unic-common 0.9.0: unmaintained Unicode data crate; final version; no exploitable attack surface"

[[IgnoredVulns]]
id = "RUSTSEC-2025-0081"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "unic-char-property 0.9.0: unmaintained Unicode data crate; final version; no exploitable attack surface"

[[IgnoredVulns]]
id = "RUSTSEC-2025-0098"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "unic-ucd-version 0.9.0: unmaintained Unicode data crate; final version; no exploitable attack surface"

[[IgnoredVulns]]
id = "RUSTSEC-2025-0100"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "unic-ucd-ident 0.9.0: unmaintained Unicode data crate; final version; no exploitable attack surface"

# ─── npm advisories (pnpm-lock.yaml) ─────────────────────────────────────────
# Reviewed 2026-06-16 / 2026-07-26 / 2026-08-12 / 2026-09-08. Most
# currently-flagged npm advisories are remediated via pnpm-workspace.yaml
# overrides (tmp, ws, form-data, protobufjs, @babel/core, dompurify→3.4.13,
# js-yaml→4.3.2, body-parser→1.20.6, joi→18.2.5, sharp→0.35.4, …). (The former
# dompurify GHSA-x4vx-rjvf-j5p4 and js-yaml-3.x GHSA-h67p-54hq-rp68 ignores
# were dropped: both advisories no longer match the lockfile after the
# overrides.) The two extract-zip ignores formerly here (GHSA-jmr9-qjv8-65gv,
# GHSA-7pqw-9j4j-h8q3) were removed 2026-09-10: pnpm-workspace.yaml's
# overrides.lighthouse now forces @lhci/cli's Lighthouse/Puppeteer chain to a
# version whose @puppeteer/browsers dependency replaced extract-zip with
# modern-tar, eliminating the package from the resolved graph entirely — see
# AUDIT.md's 2026-09-10 entry. One documented ignore remains below — no fixed
# version exists to override to for it.

# adm-zip@0.6.0 (CVE-2026-76845 / GHSA-vwc7-r8mq-g2x9, CVSS 6.8): symlink-following
# path traversal on extraction with overwrite enabled, when an attacker can
# pre-place a symlink at the extraction destination. No patched version exists
# yet (GitHub's advisory API reports first_patched_version: null) — the
# `adm-zip: ">=0.6.0"` pnpm override is a floor, not a pin: it permits any
# future adm-zip release from 0.6.0 upward, and the lockfile currently resolves
# the range to 0.6.0 because that is still the newest published version. No
# upgrade path exists either: @huggingface/transformers@4.2.0 (latest stable)
# hard-pins onnxruntime-node to an exact, mandatory (non-optional) "1.24.3",
# and even onnxruntime-node@1.29.0 (latest stable, 2026-09-10) still depends on
# adm-zip@^0.6.0 — this is not a stale-pin problem an upgrade solves. adm-zip
# is a dependency of onnxruntime-node's own postinstall/build script (which
# extracts its prebuilt native binary into
# `path.join(os.tmpdir(), 'onnxruntime-node-pkgs_<timestamp>')` — a shared,
# timestamp-named system temp directory, NOT a project-local one — verified
# directly against onnxruntime-node@1.24.3's own install-utils.js). The SOLE
# mitigation for that install-time extraction is `pnpm-workspace.yaml`'s
# `allowBuilds: { onnxruntime-node: false }`, which denies this package's
# lifecycle scripts outright, so the postinstall extraction never runs during
# a normal `pnpm install` in this repo, in CI, or in the shipped app — Vite's
# bundling behavior below is a separate fact about a different question and
# does NOT independently protect against this install-time script, since
# `pnpm install`'s lifecycle-script phase runs before Vite or any bundler ever
# resolves anything. Separately, verified 2026-09-10: even the shipped app's
# own JS code never imports the onnxruntime-node module this postinstall
# extracts a binary for — `vite.config.ts`'s `resolve.alias` hardcodes
# `@huggingface/transformers` to `dist/transformers.web.js` for every
# bundler/browser consumer (the PWA build and the Tauri WebView frontend
# alike), bypassing the package's own conditional `exports` map entirely, and
# that file contains zero real reference to onnxruntime-node (confirmed by
# inspecting the published package) — this repo's own test suite reinforces
# the same boundary by explicitly mocking that exact web-build path rather
# than letting real module resolution occur. If that `allowBuilds` entry is
# ever relaxed/removed, this exposure model must be re-evaluated — the
# temp-directory extraction target is not inherently safe on its own, and the
# Vite-alias fact does not substitute for it.

[[IgnoredVulns]]
id = "GHSA-vwc7-r8mq-g2x9"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = "adm-zip 0.6.0: symlink-following path traversal on overwrite-enabled extraction; latest release, no fixed version exists; mitigated by pnpm-workspace.yaml's allowBuilds: onnxruntime-node=false denying its postinstall/build script outright (not by the extraction path itself, which uses a shared system temp directory) — re-evaluate if that allowBuilds entry ever changes"
