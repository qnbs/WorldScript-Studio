<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- QNBS-v3: Hardened Runtime Entitlements for WorldScript Studio -->
    <!-- Required for Tauri v2 apps distributed outside the Mac App Store -->

    <!-- Allow execution of JIT-compiled code (required for WebKit) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow loading of unsigned executable memory (required for WebKit WASM) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow dyld environment variables (required for Tauri plugin loading) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Disable Library Validation to allow loading Tauri plugins -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network access for AI providers and collaboration -->
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- File access limited to app data directory (Tauri fs plugin scope) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Microphone access for voice commands -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Camera not used; explicitly omitted -->
</dict>
</plist>
