# CEF documentation ownership manifest
#
# Per ROADMAP-CEF-DESKTOP-MIGRATION.md §80.1 / Appendix M.3.
# Tier A = release/security-critical (90-day review cadence).
# Tier B = architecture/reference (180-day review cadence).
# Tier C = explanatory/how-to (as-needed).
#
# `driftCheckTool` for every entry below is intentionally "planned — not implemented"
# as of Wave 0 (see roadmap §80.1.3): building a docs:cef-check stub now would add an
# npm script and CI job with nothing real to check drift against, since no CEF code
# exists yet. Revisit at Wave 1, not silently forgotten.

documents:
  - path: docs/adr/0019-cef-desktop-runtime-strategy.md
    tier: A
    owner_role: desktop-architecture
    backup_role: cef-runtime
    last_verified:
      worldscript: "v1.27.1"
      cef: "not applicable — pre-CEF-selection"
    review_days: 90
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"

  - path: docs/cef/ROADMAP-CEF-DESKTOP-MIGRATION.md
    tier: A
    owner_role: desktop-architecture
    backup_role: cef-runtime
    last_verified:
      worldscript: "v1.27.1"
      cef: "not applicable — pre-CEF-selection"
    review_days: 90
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"

  - path: docs/cef/CEF-RISK-REGISTER.md
    tier: A
    owner_role: desktop-architecture
    backup_role: desktop-security
    last_verified:
      worldscript: "v1.27.1"
      cef: "not applicable — pre-CEF-selection"
    review_days: 90
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"

  - path: docs/cef/CEF-RUST-COMPETENCY-MATRIX.md
    tier: A
    owner_role: cef-runtime
    backup_role: rust-core
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
      # cef-competency-gate: no such CI workflow/job exists yet — planned, not implemented (CodeRabbit review finding on PR #389). Re-add once it's a real job.
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Updated in place for Wave 2 (PR #386/#387/#388/#391/#392/#393/#397/#400/#402) — 5 of 7 cef_competency items now true with linked evidence (lifetime_model_reviewed, renderer_crash_ci, crash_symbolization_smoke added); doc-sync fix flipped 2 more Appendix A.1/gate items to checked once cef-architecture-primer.md/threading-and-lifetimes.md were noticed to already have real content from PR #390 (previously left unchecked on a stale 'no dedicated doc yet' annotation); Wayland smoke checked in Appendix A.1 (PR #393); accessibility_smoke stays false (PR #397 proved state enablement only, not tree observability); crash_symbolization_smoke flipped true (PR #400 — self-induced crash in our own code resolved end-to-end via dump_syms + minidump-stackwalk, neither tool needs a Chromium checkout despite the earlier assumption; Chromium-internal frames remain genuinely unsymbolized, no distribution ships debug symbols); PR #402: sandbox development plan validated (real feasibility test succeeds on the CI runner, explicit acceptance bar documented — sandbox_smoke itself stays false) AND the Linux-dependency gate item split into a Wave-2-scoped half (now checked — PR #395's inventory+ldd work was already complete, just conflated with a different later goal) and a separate packaged-installer half (stays unchecked, correctly later scope) — competency gate still not satisfied (10/13, was 8/12 before the split; sandbox_smoke, accessibility-tree observability, Linux packaged-installer declaration, upgrade playbook remain open)."

  - path: docs/cef/TAURI-COUPLING-INVENTORY.md
    tier: B
    owner_role: desktop-architecture
    backup_role: rust-core
    last_verified:
      worldscript: "v1.27.1"
      cef: "not applicable"
    review_days: 180
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"

  - path: docs/adr/0020-cef-binding-choice-thin-cpp-host.md
    tier: A
    owner_role: cef-runtime
    backup_role: desktop-architecture
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"

  - path: docs/cef/CEF-BINDING-DECISION-SCORECARD.md
    tier: A
    owner_role: cef-runtime
    backup_role: desktop-architecture
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Appendix H scorecard, filled in per ADR-0020 — re-score triggers are listed in the doc itself, not just the review_days cadence."

  - path: docs/cef/knowledge/cef-architecture-primer.md
    tier: A
    owner_role: cef-runtime
    backup_role: rust-core
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Real evidence from PR #388 for process model, message loop, subprocess packaging; crash reporting proven in CI (PR #392); Wayland display-server smoke proven in CI (PR #393); accessibility state enablement proven in CI, zero regression (PR #397); crash symbolization proven for our own code's frames (PR #400) — Chromium/CEF-internal frames remain unsymbolized, honestly (no distribution ships debug symbols, verified against CEF's own build index). Sandbox config, a real GPU/compositor matrix, accessibility-tree observability (AT-SPI), and a directly-observed process-tree snapshot remain open."

  - path: docs/cef/knowledge/cef-rust-binding-cookbook.md
    tier: A
    owner_role: rust-core
    backup_role: cef-runtime
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Real evidence from apps/desktop-cef/rust-core/ (PR #388) — but the FFI surface is one trivial function, not representative of real API coverage."

  - path: docs/cef/knowledge/threading-and-lifetimes.md
    tier: A
    owner_role: cef-runtime
    backup_role: rust-core
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
      # cef-competency-gate: no such CI workflow/job exists yet — planned, not implemented (CodeRabbit review finding on PR #389). Re-add once it's a real job.
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Real evidence from PR #388 for UI-thread callback discipline and CEF ref-counting/callback-lifetime patterns. IO-thread and render-process-side rules untouched; full dual-review (§4.11.4) not yet done."

  - path: docs/cef/knowledge/subprocess-and-shutdown.md
    tier: A
    owner_role: cef-runtime
    backup_role: rust-core
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Real, CI-run evidence (PR #388) — test (scripts/cef/run-launch-cycle-proof.mjs) -> CI job (cef-learning-harness) -> doc, per §61.1.4. Save-coordinator/window-state steps remain unimplemented Wave 5+ scope."

  - path: docs/cef/knowledge/linux-runtime-notes.md
    tier: A
    owner_role: cef-runtime
    backup_role: desktop-architecture
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Dev-machine spike (ADR-0020) plus real CI-run evidence (PR #388, GitHub Actions ubuntu-latest) — two machines now, one GPU config each, X11 only; not a compatibility contract yet."

  - path: docs/cef/knowledge/debugging-and-crash-playbook.md
    tier: A
    owner_role: desktop-security
    backup_role: cef-runtime
    last_verified: null
    review_days: 90
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Skeleton only — Status: Not started"

  - path: docs/cef/knowledge/binding-upgrade-playbook.md
    tier: A
    owner_role: rust-core
    backup_role: cef-runtime
    last_verified: null
    review_days: 90
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Skeleton only — Status: Not started"

  - path: docs/cef/UI-DOMAIN-STATE-CLASSIFICATION.md
    tier: B
    owner_role: desktop-architecture
    backup_role: cef-runtime
    last_verified:
      worldscript: "v1.27.1"
      cef: "not applicable — pre-CEF-selection"
    review_days: 180
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"

  - path: docs/architecture/native-readiness.md
    tier: B
    owner_role: desktop-architecture
    backup_role: desktop-security
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 180
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Living scorecard — re-score at every architecture-changing PR and wave exit (§7.4.5), not just on a review-day cadence."

# Owner/backup roles above are role placeholders (roadmap §80.1.8: prefer role/subsystem
# ownership over one person's name, e.g. via CODEOWNERS mapping). Assigning real
# maintainer handles to each role is Wave 1+ operational work, not a Wave 0 blocker.
roles:
  desktop-architecture: "unassigned — maps to whoever owns ADR-0019 and the roadmap"
  cef-runtime: "unassigned — maps to whoever implements the CEF host (Wave 2+)"
  rust-core: "unassigned — maps to whoever owns worldscript-core/-storage/-crypto"
  desktop-security: "unassigned — maps to whoever owns the CEF security review (Wave 14)"
