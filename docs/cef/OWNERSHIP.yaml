# CEF documentation ownership manifest
#
# Per ROADMAP-CEF-DESKTOP-MIGRATION.md §80.1 / Appendix M.3.
# Tier A = release/security-critical (90-day review cadence).
# Tier B = architecture/reference (180-day review cadence).
# Tier C = explanatory/how-to (as-needed).
#
# `driftCheckTool` for every entry below is intentionally "planned — not implemented"
# as of Wave 0 (see roadmap §80.1.3): building a docs:cef-check stub now would add an
# npm script and CI job with nothing real to check drift against, since no CEF code
# exists yet. Revisit at Wave 1, not silently forgotten.

documents:
  - path: docs/adr/0019-cef-desktop-runtime-strategy.md
    tier: A
    owner_role: desktop-architecture
    backup_role: cef-runtime
    last_verified:
      worldscript: "v1.27.1"
      cef: "not applicable — pre-CEF-selection"
    review_days: 90
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"

  - path: docs/cef/ROADMAP-CEF-DESKTOP-MIGRATION.md
    tier: A
    owner_role: desktop-architecture
    backup_role: cef-runtime
    last_verified:
      worldscript: "v1.27.1"
      cef: "not applicable — pre-CEF-selection"
    review_days: 90
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"

  - path: docs/cef/CEF-RISK-REGISTER.md
    tier: A
    owner_role: desktop-architecture
    backup_role: desktop-security
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Wave 2 checkpoint (2026-08-19, external review feedback): R-06 (CEF/Rust/C++ lifetime defects) and R-13 (accessibility regression) had drifted — real Wave 2 implementation/CI evidence existed for both while they stayed *unassigned*/OPEN, past the point this register's own rule allows ('assign before the corresponding wave begins, not before' — Wave 2 has begun). Both assigned real role-based owners and moved to MITIGATING with linked evidence; the remaining rows correctly stay unassigned/OPEN since their waves haven't begun. Wave 2 checkpoint (2026-08-20, PR #404): new row R-19 added — real sandbox enforcement uncovered a real, separate regression in renderer crash-dump generation under sandbox; assigned MITIGATING (real diagnostic evidence narrows the cause, but not proven/fixed) with role-based owner cef-runtime, added the same wave it was discovered."

  - path: docs/cef/CEF-RUST-COMPETENCY-MATRIX.md
    tier: A
    owner_role: cef-runtime
    backup_role: rust-core
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
      # cef-competency-gate: no such CI workflow/job exists yet — planned, not implemented (CodeRabbit review finding on PR #389). Re-add once it's a real job.
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Updated in place for Wave 2 (PR #386/#387/#388/#391/#392/#393/#397/#400/#402) — 5 of 7 cef_competency items now true with linked evidence (lifetime_model_reviewed, renderer_crash_ci, crash_symbolization_smoke added); doc-sync fix flipped 2 more Appendix A.1/gate items to checked once cef-architecture-primer.md/threading-and-lifetimes.md were noticed to already have real content from PR #390 (previously left unchecked on a stale 'no dedicated doc yet' annotation); Wayland smoke checked in Appendix A.1 (PR #393); accessibility_smoke stays false (PR #397 proved state enablement only, not tree observability); crash_symbolization_smoke flipped true (PR #400 — self-induced crash in our own code resolved end-to-end via dump_syms + minidump-stackwalk, neither tool needs a Chromium checkout despite the earlier assumption; Chromium-internal frames remain genuinely unsymbolized, no distribution ships debug symbols); PR #402: sandbox development plan validated (real feasibility test succeeds on the CI runner, explicit acceptance bar documented — sandbox_smoke itself stays false); the Linux-dependency gate item split into a Wave-2-scoped half (now checked — PR #395's inventory+ldd work was already complete, just conflated with a different later goal) and a separate packaged-installer half (stays unchecked, correctly later scope); and binding-upgrade-playbook.md written proactively with a real 15-step executable procedure (resolving a circular dependency — the gate required a playbook that could previously only be written after the first upgrade it was meant to gate), so 'upgrade playbook exists'/'Upgrade playbook written' both flip to checked — competency gate at that point stood at 11/13. PR #404 (2026-08-20): the real sandbox-enable follow-up — sandbox_smoke flips true (renderer-specific evidence: real Seccomp=2 on 3/3 observed renderers, real chrome-sandbox setuid invocation, zero weakening flags, zero regression to existing proofs), but this uncovered a new, real, separate regression in renderer crash-dump generation under sandbox — new field crashpad_renderer_dump_sandboxed added (false/open) rather than silently folding it into sandbox_smoke's own claim. Two new gate items added to match (one checked, one open) — gate now 12/15, still not satisfied; sandbox/Crashpad coexistence (not sandbox enforcement alone), accessibility-tree observability, and Linux packaged-installer declaration remain open. See CEF-RISK-REGISTER.md's new R-19."

  - path: docs/cef/TAURI-COUPLING-INVENTORY.md
    tier: B
    owner_role: desktop-architecture
    backup_role: rust-core
    last_verified:
      worldscript: "v1.27.1"
      cef: "not applicable"
    review_days: 180
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"

  - path: docs/adr/0020-cef-binding-choice-thin-cpp-host.md
    tier: A
    owner_role: cef-runtime
    backup_role: desktop-architecture
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"

  - path: docs/cef/CEF-BINDING-DECISION-SCORECARD.md
    tier: A
    owner_role: cef-runtime
    backup_role: desktop-architecture
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Appendix H scorecard, filled in per ADR-0020 — re-score triggers are listed in the doc itself, not just the review_days cadence."

  - path: docs/cef/knowledge/cef-architecture-primer.md
    tier: A
    owner_role: cef-runtime
    backup_role: rust-core
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Real evidence from PR #388 for process model, message loop, subprocess packaging; crash reporting proven in CI (PR #392); Wayland display-server smoke proven in CI (PR #393); accessibility state enablement proven in CI, zero regression (PR #397); crash symbolization proven for our own code's frames (PR #400) — Chromium/CEF-internal frames remain unsymbolized, honestly (no distribution ships debug symbols, verified against CEF's own build index). PR #404 (2026-08-20): renderer sandbox enforcement proven in CI (real Seccomp=2, real chrome-sandbox setuid invocation) — the process-tree snapshot is now directly observed, not inferred (upgraded from the earlier PR #388 caveats). But the same PR found a real, separate, currently-open regression: renderer crash-dump generation fails once the sandbox is genuinely active (evidence-backed PID-namespace/ptrace-access-control hypothesis, not proven to the exact-PID level — see the doc's own carefully-worded status). A real GPU/compositor matrix, accessibility-tree observability (AT-SPI), and a stock-Linux-desktop reproduction of the sandboxed-crash-dump regression remain open."

  - path: docs/cef/knowledge/cef-rust-binding-cookbook.md
    tier: A
    owner_role: rust-core
    backup_role: cef-runtime
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Real evidence from apps/desktop-cef/rust-core/ (PR #388) — but the FFI surface is one trivial function, not representative of real API coverage."

  - path: docs/cef/knowledge/threading-and-lifetimes.md
    tier: A
    owner_role: cef-runtime
    backup_role: rust-core
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
      # cef-competency-gate: no such CI workflow/job exists yet — planned, not implemented (CodeRabbit review finding on PR #389). Re-add once it's a real job.
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Real evidence from PR #388 for UI-thread callback discipline and CEF ref-counting/callback-lifetime patterns. IO-thread and render-process-side rules untouched; full dual-review (§4.11.4) not yet done."

  - path: docs/cef/knowledge/subprocess-and-shutdown.md
    tier: A
    owner_role: cef-runtime
    backup_role: rust-core
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Real, CI-run evidence (PR #388) — test (scripts/cef/run-launch-cycle-proof.mjs) -> CI job (cef-learning-harness) -> doc, per §61.1.4. Save-coordinator/window-state steps remain unimplemented Wave 5+ scope."

  - path: docs/cef/knowledge/linux-runtime-notes.md
    tier: A
    owner_role: cef-runtime
    backup_role: desktop-architecture
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Dev-machine spike (ADR-0020) plus real CI-run evidence (PR #388, GitHub Actions ubuntu-latest) — two machines now, one GPU config each, X11 only; not a compatibility contract yet."

  - path: docs/cef/knowledge/debugging-and-crash-playbook.md
    tier: A
    owner_role: desktop-security
    backup_role: cef-runtime
    last_verified: null
    review_days: 90
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Skeleton only — Status: Not started"

  - path: docs/cef/knowledge/binding-upgrade-playbook.md
    tier: A
    owner_role: rust-core
    backup_role: cef-runtime
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 90
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Written proactively (PR #402), not left blank pending the first real upgrade — resolves a circular dependency (the competency gate required this doc to exist before Wave 4, but it could previously only be written after the first upgrade it was meant to gate). 15-step executable procedure mapping to real scripts/CI steps from Wave 2's own proof work (SDK pin/fetch/verify, build, lifecycle harness, sandbox, crash, symbolization, accessibility, X11, Wayland, dependency/linkage diff, docs, rollback). Not yet exercised against a real upgrade — must be enriched with what actually broke the first time one happens, honestly noted in the doc itself."

  - path: docs/cef/UI-DOMAIN-STATE-CLASSIFICATION.md
    tier: B
    owner_role: desktop-architecture
    backup_role: cef-runtime
    last_verified:
      worldscript: "v1.27.1"
      cef: "not applicable — pre-CEF-selection"
    review_days: 180
    related_ci: []
    driftCheckTool: "planned — not implemented, see Wave 1"

  - path: docs/architecture/native-readiness.md
    tier: B
    owner_role: desktop-architecture
    backup_role: desktop-security
    last_verified:
      worldscript: "v1.27.1"
      cef: "151.3.18+gbeff58d+chromium-151.0.7922.138"
    review_days: 180
    related_ci:
      - cef-learning-harness
    driftCheckTool: "planned — not implemented, see Wave 1"
    note: "Living scorecard — re-score at every architecture-changing PR and wave exit (§7.4.5), not just on a review-day cadence. Re-scored 2026-08-20 (PR #404): Sandbox posture flips to PASS — renderer-specific enforcement only (real Seccomp=2, real chrome-sandbox setuid invocation); Crash reporting row gains an explicit caveat — renderer crash-dump generation under the now-real sandbox is a new, separate, real regression, NOT folded into that row's PASS. Higher PASS count explicitly not read as Wave 2 being closer to done — sandbox/Crashpad coexistence, not sandbox enforcement alone, is the real remaining item."

# Owner/backup roles above are role placeholders (roadmap §80.1.8: prefer role/subsystem
# ownership over one person's name, e.g. via CODEOWNERS mapping). Assigning real
# maintainer handles to each role is Wave 1+ operational work, not a Wave 0 blocker.
roles:
  desktop-architecture: "unassigned — maps to whoever owns ADR-0019 and the roadmap"
  cef-runtime: "unassigned — maps to whoever implements the CEF host (Wave 2+)"
  rust-core: "unassigned — maps to whoever owns worldscript-core/-storage/-crypto"
  desktop-security: "unassigned — maps to whoever owns the CEF security review (Wave 14)"
