---
description: Require deterministic Vitest mocks and CI-first heavy suites per docs/CI.md
globs: tests/**/*,packages/**/tests/**/*,components/**/*.test.ts,components/**/*.test.tsx,vitest.config.ts,playwright.config.ts,stryker.config.mjs,stryker-scope.json,.github/workflows/mutation.yml,scripts/stryker-scope.mjs,scripts/stryker-scope.d.mts,scripts/aggregate-stryker-reports.mjs,scripts/aggregate-stryker-reports.d.mts,scripts/assert-ci-only.mjs
alwaysApply: false
---

# Testing (Vitest + Playwright)

## Determinismus

- Tests **isoliert**: keine Reihenfolge-, Netzwerk- oder `Date.now()`-Abhängigkeit ohne Mock/Fake-Timer.
- Bei `Intl.Segmenter`, `Intl.PluralRules` und verwandten ICU-APIs stabile Verhaltensgarantien statt exakt formatierter, engine-/locale-abhängiger Zählwerte prüfen.
- Redux, `localStorage`, IDB: Reset via `tests/setup.ts` oder explizite Fixtures.
- Vitest: **`maxWorkers: 1`** im Repo — keinen parallelen schweren Lauf neben Build/E2E auf Low-RAM-Maschinen.

## Mocks

- `@google/genai`, `@ai-sdk/*`, Tauri, `fetch`, schwere ML-Peers: **`vi.mock`** / Aliase wie in `vitest.config.ts`.
- Keine echten API-Keys oder Produktions-URLs.

## Verbotene „Grün-Tricks“

- Kein Auskommentieren von Assertions, kein dauerhaftes Ignorieren flakiger Unit-Tests.
- `it.skip`/`describe.skip` oder eine temporäre Quarantäne nur nach dem vollständigen Vertrag in
  [`docs/CI.md#first-attempt-failures-and-flake-quarantine`](../../docs/CI.md#first-attempt-failures-and-flake-quarantine)
  — nicht als Ersatz für einen Fix.

## Assertions & E2E-Hilfen

- Interaktion: `@testing-library/user-event`; async: `findBy*`/`waitFor`.
- E2E: [`tests/e2e/helpers.ts`](../../tests/e2e/helpers.ts) (SPA-ready, kein `networkidle` unter Vite).
- **axe:** schwere/critical Verstöße in `tests/e2e/a11y.spec.ts` und erweiterten Routen vermeiden — [`docs/ACCESSIBILITY.md`](../../docs/ACCESSIBILITY.md).

## Spezialfälle

- `sceneRevisionService` / reine IDB: `@vitest-environment node` + `IDBFactory` + `_resetDbForTest()` (see `tests/CLAUDE.md`).
- Risiko-Hotspots bei Änderungen testen: `dbService`, `aiProviderService`, Import/Export, `storageBackend`.

## CI-first (kanonisch: [`docs/CI.md`](../../docs/CI.md))

| Tier | Befehle |
|------|---------|
| **Lokal schnell** | `pnpm run ci:prepush`; optional focused `pnpm exec vitest run <path>` **ohne** `--coverage` |
| **CI schwer** | Vitest **mit** Coverage (thresholds defined by [`scripts/coverage-thresholds.json`](../../scripts/coverage-thresholds.json); do not duplicate or lower them here), `CI=true pnpm run test:e2e`, LHCI, `bundle:budget` |

- Merge-Bar = **grüner GitHub-Workflow**, nicht voller lokaler E2E auf schwacher Hardware.
- Playwright E2E is CI-only on this constrained workstation; use `playwright.config.ts` for CI matrix details.
- **Stryker:** CI/manual only; never lower a score gate or delete tests to make it green.

<example>
`aiProviderService`: `vi.mock` für SDK; feste Antwort; bei IDB-Test `node`-Environment + Factory-Reset.
</example>

<example type="invalid">
Flaky Test auskommentieren ohne Ticket; Coverage-Schwelle senken statt Test hinzufügen; E2E mit echtem Gemini-Key.
</example>
